---
import Brain from "@assets/Brain.svg";
import { Image } from "astro:assets";
---

<div class="flex flex-col justify-center items-center w-full max-w-7xl mx-auto px-6 py-16 overflow-hidden bg-transparent">
  
  <div class="w-full">
    {/* Versión Mobile */}
    <div class="reveal opacity-0 sm:hidden mb-12 text-center" data-animation="pop-in">
      <Image src={Brain} alt="Brain" class="w-56 h-auto mx-auto" loading="lazy" />
    </div>

    {/* Desktop Grid */}
    <div class="hidden sm:grid grid-cols-[minmax(0,0.72fr)_minmax(420px,1.56fr)_minmax(0,0.72fr)] w-full items-center gap-6 lg:gap-8 mb-24">
      
      {/* 1. Lado Izquierdo: Analytical Thinking */}
      <div 
        class="reveal opacity-0 flex flex-col items-end text-right self-center transition-transform duration-500 hover:scale-105 cursor-default group"
        data-animation="slide-left"
        style="transition-delay: 0.1s;"
      >
        {/* Forzamos que el título ocupe un espacio fijo para que la línea y el párrafo empiecen donde mismo */}
        <h3 class="text-xl md:text-3xl text-freshgreen font-bold leading-tight group-hover:text-pure transition-colors duration-300 min-h-[3rem] flex items-end justify-end">
          Analytical thinking
        </h3>
        <div class="w-12 h-1 bg-secondary/30 my-4 rounded-full transition-all duration-300 group-hover:w-20 group-hover:bg-secondary ml-auto"></div>
        <p class="text-birch font-semibold text-lg">Experimental lobe</p>
        <p class="text-birch/75 text-sm md:text-base leading-relaxed max-w-[260px]">
          AI model trained on in-house experimental data
        </p>
      </div>

      {/* 2. Centro: Cerebro */}
      <div 
        class="reveal opacity-0 flex justify-center items-center" 
        data-animation="pop-in" 
        style="transition-delay: 0.3s;"
      >
        <Image
          src={Brain}
          alt="Brain Diagram"
          class="w-full max-w-sm lg:max-w-md h-auto object-contain transition-transform duration-1000 hover:scale-110"
        />
      </div>

      {/* 3. Lado Derecho: Creative Thinking */}
      <div 
        class="reveal opacity-0 flex flex-col items-start text-left self-center transition-transform duration-500 hover:scale-105 cursor-default group"
        data-animation="slide-right"
        style="transition-delay: 0.5s;"
      >
        {/* Misma altura mínima que el de la izquierda para nivelar */}
        <h3 class="text-xl md:text-3xl text-freshgreen font-bold leading-tight group-hover:text-pure transition-colors duration-300 min-h-[2rem] flex items-end justify-start">
          Creative thinking
        </h3>
        <div class="w-12 h-1 bg-secondary/30 my-4 rounded-full transition-all duration-300 group-hover:w-20 group-hover:bg-secondary mr-auto"></div>
        <p class="text-birch font-semibold text-lg">In silico lobe</p>
        <p class="text-birch/75 text-sm md:text-base leading-relaxed max-w-[260px]">
          AI model trained on public experimental data
        </p>
      </div>
    </div>
  </div>

  {/* Sección Inferior */}
  <div class="flex flex-col w-full gap-10 text-center items-center py-4 mt-12">
    <div class="reveal opacity-0" data-animation="slide-up" style="transition-delay: 0.1s;">
      <h1 class="font-roboto font-medium text-4xl leading-normal tracking-widest uppercase transition-transform duration-300 hover:scale-105 cursor-default inline-block">
        HOW IT WORKS
      </h1>
    </div>
    <div class="reveal opacity-0" data-animation="slide-up" style="transition-delay: 0.3s;">
      <h2 class="text-freshgreen text-[32px] md:text-[36px] font-inter tracking-wider font-semibold">
        MokaDex integrates nature's knowledge with cutting edge technology
      </h2>
    </div>
    <div class="reveal opacity-0" data-animation="slide-up" style="transition-delay: 0.5s;">
      <p class="text-birch/75 text-[20px] md:text-[22px] font-inter font-medium leading-relaxed max-w-4xl">
        MokaDex is our proprietary AI engine that serves as the digital architect for Moka’s Operating System. 
        It identifies novel bioactive molecules and their mechanisms of action with unprecedented speed.
      </p>
    </div>
  </div>
</div>

<script>
  const observerOptions = { threshold: 0.1, rootMargin: '0px 0px -50px 0px' };
  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        const animationType = entry.target.getAttribute('data-animation');
        entry.target.classList.add(`animate-${animationType}`);
        entry.target.classList.remove('opacity-0');
        observer.unobserve(entry.target);
      }
    });
  }, observerOptions);
  document.querySelectorAll('.reveal').forEach((el) => observer.observe(el));
</script>

<style>
  .animate-slide-left { animation: slideLeft 0.8s ease-out forwards; }
  .animate-slide-right { animation: slideRight 0.8s ease-out forwards; }
  .animate-slide-up { animation: slideUp 0.8s ease-out forwards; }
  .animate-pop-in { animation: popIn 1s cubic-bezier(0.16, 1, 0.3, 1) forwards; }

  @keyframes slideLeft { from { opacity: 0; transform: translateX(-40px); } to { opacity: 1; transform: translateX(0); } }
  @keyframes slideRight { from { opacity: 0; transform: translateX(40px); } to { opacity: 1; transform: translateX(0); } }
  @keyframes slideUp { from { opacity: 0; transform: translateY(30px); } to { opacity: 1; transform: translateY(0); } }
  @keyframes popIn { from { opacity: 0; transform: scale(0.95); } to { opacity: 1; transform: scale(1); } }
</style>
