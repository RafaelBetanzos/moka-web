<div class="bg-hero-pattern bg-cover min-h-screen bg-center w-full flex items-center overflow-hidden">
  <div class="flex flex-row items-center justify-end ml-[40%] md:ml-[50%] xl:ml-[60%] font-medium animate-moka-reveal">
    <p class="font-inter text-secondary sm:text-7xl text-4xl leading-tight xl:pr-[40%]">
      The operating system for <strong class="font-bold">natural</strong> product discovery.
    </p>
  </div>
</div>

<style>
  /* Animación sutil de aparición desde la derecha */
  .animate-moka-reveal {
    animation: mokaReveal 1.4s cubic-bezier(0.16, 1, 0.3, 1) forwards;
    opacity: 0;
    will-change: transform, opacity;
  }

  @keyframes mokaReveal {
    from {
      opacity: 0;
      transform: translateX(40px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }
</style>